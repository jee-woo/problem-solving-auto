-- 코드를 작성해주세요
SELECT I.ITEM_ID, I.ITEM_NAME, I.RARITY
FROM ITEM_INFO I
WHERE NOT EXISTS (
    SELECT 1
    FROM ITEM_TREE T
    WHERE I.ITEM_ID = T.PARENT_ITEM_ID
)
ORDER BY I.ITEM_ID DESC

/*

0
|\
1 2
|\
3 4

PARENT_ITEM_ID에 없는 애들

*/